apiVersion: v1
kind: Secret
metadata:
  name: "{{ include "dvp-cloud-controller-manager.name" . }}-kubeconfig"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dvp-cloud-controller-manager.labels" . | nindent 4 }}
data:
  kubeconfig: {{ .Values.kubeconfigDataBase64 }}